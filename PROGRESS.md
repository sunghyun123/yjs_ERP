# 영전사 ERP 개발 진행 기록

> 최종 업데이트: 2026-07-21 (날짜 입력 6/31 버그 수정 — 공용 DateField, fix/date-field-badinput)
>
> 이 문서는 2층 구조다.
> - **완료된 작업** = 무엇을 했는지 한 줄씩, 전체 기록(검색·추적용).
> - **주요 엔지니어링 결정** = 왜 그렇게 했는지·무엇에 데었는지(포트폴리오·심층 분석용).
> - 굵직한 항목은 표에서 `→` 표시로 심층 섹션을 가리킨다.

---

## 완료된 작업

| # | 기능 | 요약 |
| --- | --- | --- |
| 1 | DB 스키마 + Supabase 세팅 | 8개 테이블, RLS |
| 2 | 인증 + 레이아웃 | 로그인/로그아웃, 보호 레이아웃 |
| 3 | 대시보드 홈 (`/`) | KPI 카드 · 손익 차트 · 진행 공사 목록 |
| 4 | 투입실적 입력 (`/input`) | 자동완성 · 실시간 계산 · 저장/수정 |
| 5 | 매출손익 현황 (`/sales`) | 연도별 KPI + 월별 차트 + 상세 테이블 |
| 6 | 초기 마이그레이션 | 수주 518 + 기성 192 + 거래처 24 |
| 7 | 수주대장 계산 버그 수정 | 보험료율·하도전용율 JOIN 경로 → 수주 직접 컬럼 |
| 8 | VPS 배포 | `erp.yjsboard.com`, PM2 + Nginx + SSL |
| 9 | 성과금액 소스 변경 | 기성/준공 → 공사이력 (진행기준 수익인식) |
| 10 | 투입금액 계산식 변경 | `calc투입금액` → `calc합계(×1.06)` |
| 11 | 마이그레이션 | 투입실적 312 + 공사이력 263건 |
| 12 | 투입실적 현황 탭 | DataTable + Sheet 수정/삭제 |
| 13 | 투입실적 현황 UX | 날짜 범위 필터 · 검색어 유지 · useEffect 동기화 |
| 14 | 투입실적 입력 2단 레이아웃 | 우측 sticky 계산 패널 |
| 15 | 수주 등록·수정·삭제 폼 | Sheet → Dialog 900px |
| 16 | 차트 범례 순서 고정 | 성과→투입→손익 + 헤더 연간 합계(백만원) |
| 17 | SearchableSelect 3종 버그 수정 | → [Radix Dialog 내 portal 드롭다운](#searchableselect--radix-dialog-내-portal-드롭다운-3종-버그) |
| 18 | 수주금액 계산 패널 UX | 너비 확장 · 하도적용 히어로 카드 · 필수 별표 |
| 19 | 수주대장 UX | 항상-표시 수정 버튼 · 삭제 확인 위치 · 진행상태 이동 |
| 20 | 수주 Dialog 탭 구조 | [기본/기성/준공] 3탭 + 기성 CRUD |
| 21 | 공사이력 페이지 (`/progress`) | 입력 탭(달성율 역산) + 현황 탭(필터·검색·CRUD) |
| 22 | 공사이력 현황 UI 통일 | 투입실적 패턴으로 맞춤 |
| 23 | 투입실적 메뉴명 변경 | + 입력 시 마지막 투입일 표시 |
| 24 | 달성률 이중 표시 | 공정 달성률(공사이력) vs 기성 달성률(기성 청구) |
| 25 | 투입실적 입력 화면 UI/UX | 정렬 · 인풋 강화 · 지브라/호버 |
| 26 | 홈 KPI 개선 | 계획금액 제거, 전월 동기간 대비 성과 추가 |
| 27 | ERP 미입력 공사 섹션 | `dashboard_공사` 수신 · 소프트 삭제 · 페이지네이션 |
| 28 | `POST /api/dashboard-sync` | Bearer 인증, upsert |
| 29 | 이력/실적 빠른 입력 | 미입력 공사 → 사전 선택된 채 이동 |
| 30 | 공무 보고서 (`/gongmu`) 재설계 | 3→2단계 네비, ISO year 경계 버그 수정 |
| 31 | 공무 주간보고서 | 월간현황 통합카드, total 페이지 삭제 |
| 32 | 공무 보고서 버그 수정 | 월이동 초기화 · 자동가져오기 제거 · 검색 추가 |
| 33 | 공사검색 드롭다운 UX | 통합 셀 + createPortal로 overflow 잘림 해결 |
| 34 | 금액 입력란 천단위 쉼표 포맷 | 포커스 시 숫자 · 블러 시 콤마 |
| 35 | 거래처 관리 (`/admin/clients`) | DataTable + Sheet CRUD |
| 36 | 공사단가 관리 (`/admin/rates`) | 인라인 편집 CRUD |
| 37 | 공무담당자 관리 (`/admin/gongmu`) | 이름·등록일 CRUD |
| 38 | 공무 페이지 개선 6종 | 드롭다운 flip · 검색 · 기본주차 · 정렬 등 |
| 39 | 매출손익 공사별 피벗 단순화 | 과기능 제거, `table-fixed`+`colgroup` 컬럼 고정 |
| 40 | 매출손익 마이그레이션 스크립트 | (후속 #54에서 삭제됨) |
| 41 | 매출손익 데이터 정합성 | → [구ERP 0원 차이 달성](#데이터-정합성--성과금액-일별-증분-정본화) |
| 42 | 파일명 통일 | 매출손익현황.xlsx → 매출손익.xlsx |
| 43 | KpiCards 버그 3건 수정 | 월말 누락 · 타임존 · 전월 동기간 환산 |
| 44 | 매출손익 공사별 표 개편 | 1공사 3행 그룹(지표 열), 원 단위, sticky 스크롤 |
| 45 | 모바일 레이아웃 정비 6종 | 패딩·그리드·표 컬럼폭 반응형화 |
| 46 | `formatEok` 천단위 콤마 | 공통 포맷터 일괄 반영 |
| 47 | 수주대장 팝업 통합 | 상세 Sheet 제거, 행 클릭 → 수정 Dialog |
| 48 | 삭제 확인 UI 위치 재수정 | #19 회귀 버그 복원 |
| 49 | 공무 금주실적 월 이동 버그 수정 | 동적 주차 전환(`displayWeekEntry`) |
| 50 | 카카오 단일 로그인 + 화이트리스트 | → [카카오 OAuth 인증](#카카오-oauth-단일-로그인--화이트리스트) |
| 51 | 투입실적 상세 테이블 전환 | `투입구분` 기반 동적 행 |
| 52 | 상세 기반 투입금액/손익 계산 | + `/admin/*` admin 전용 권한 보호 |
| 53 | Dashboard 월간 KPI API | `monthly-kpi.ts` 분리 + Bearer API |
| 54 | 성과금액 일별 증분 정본화 | → [데이터 정합성](#데이터-정합성--성과금액-일별-증분-정본화) |
| 55 | 현황 검색 금액 합계 footer | 필터 전체 행 합산(페이지 무관), 수주대장·투입실적 |
| 56 | 백업/포트폴리오 증거 체계 | `backup:data` 스케줄러 + PostHog 초안 |
| 57 | 안정성 개선 5종 | 검색어 quote · 에러 표면화 · 배치 upsert 등 |
| 58 | 날짜 KST 고정 | → [KST 타임존 고정](#날짜-kst-고정) |
| 59 | RLS 화이트리스트 강제 (방향 A) | → [RLS 방향 A](#rls-방향-a--앱-우회-직접-호출-차단) |
| 60 | RLS admin 쓰기 제한 | `is_admin()`, 거래처·공사단가 쓰기 admin 전용 |
| 61 | 에러 모니터링 도입 | PostHog 3계층 캡처(클라/서버/렌더) |
| 62 | 공사현장 드롭다운 + 준공 자동화 | → [준공 자동화·두 달성률](#준공-자동화--두-달성률-구분) |
| 63 | 공사이력 성과 원/% 토글 입력 | 운영 피드백 — 정본은 항상 원(원/% 환산은 순수 헬퍼) |
| 64 | 변경내역 추적 (`/admin/updates`) | → [audit_log 트리거 결정](#변경내역-추적--db-트리거-vs-앱레벨-로깅) |
| 65 | 공사이력 % 입력 = 누적 달성률 | → [누적 입력 재해석](#공사이력--입력--누적-달성률로-재해석) |
| 66 | 입력 화면 선택 공사 이력 목록 + % 기준 정리 | → [선택 공사 이력 목록·% 기준 정리](#선택-공사-이력-목록--입력칸--기준-정리) |
| 67 | DB 백업 운영 적용 (cron 라이브) | → [DB 백업 운영 적용](#db-백업-운영-적용--ipv6-pooler-docker-풀백업) |
| 68 | 매출손익 성과에서 준공정산 제외 | → [매출손익 성과에서 준공정산 제외](#매출손익-성과에서-준공정산-제외) |
| 69 | 레포 위생 + DB 타입 never-collapse 수리 | → [DB 타입 never-collapse 수리](#db-타입-never-collapse-수리--as-any-44곳의-진짜-원인) |
| 70 | effect 동기 setState 8건 전량 해소 | → [파생 상태 수리](#effect-동기-setstate-8건--파생-상태-수리) |
| 71 | lint 마감 — exhaustive-deps 2건 + 미사용 disable 8건 | → [hooks deps 계약](#hooks-deps-계약--exhaustive-deps-2건-수리) |
| 72 | 준공·공사이력 완전 분리 (달성률 127.19% 해소) | → [준공·공사이력 완전 분리](#준공공사이력-완전-분리--달성률-12719-버그의-구조적-해소) |
| 73 | 수주대장 공급가/하도적용 토글 + 준공액 컬럼 (PR #11) | 관대 하도적용을 `calc하도적용표시금액` 헬퍼로 분리, 미니 준공검사보고서 박스, TanStack 행 캐시 함정 수리 |
| 74 | 대시보드 유형별 도넛 실데이터 연결 (PR #12) | → [유형별 도넛 실데이터](#대시보드-유형별-도넛--실데이터-연결) |
| 75 | 자재관리 페이지 (`/materials`) (PR #13) | → [자재관리 — 기록에서 파생하는 재고](#자재관리-페이지--기록에서-파생하는-재고) |
| 76 | 도넛 연도 필터 + 상태 재정의 + 조각 클릭 팝업 | → [도넛 상태 재정의·클릭 팝업](#도넛-연도-필터--상태-재정의--조각-클릭-팝업) |
| 77 | 날짜 입력(6/31) 버그 수정 — 공용 DateField (PR 머지) | → [날짜 입력 — controlled가 스스로 낫는 이유](#날짜-입력631--controlled가-스스로-낫는-이유로-수정-범위-112) |

> 표기: `→`는 아래 **주요 엔지니어링 결정** 섹션의 심층 설명을 가리킨다.

**남은 작업:**
- **배포 후 재확인(#72 후속):** 구코드 배포본이 준공 처리 시 준공정산 행을 되살릴 수 있음 → 9613316 배포 직후 `공사이력.준공정산=true` 잔여 행 0건 확인
- **도넛(#76 후속):** `feat/donut-year-filter` → main PR·머지, 배포 후 사장님 실화면 컨펌. (#74의 "미분류 과다" 우려는 #76 상태 재정의로 해소 — 시공상태 컬럼은 도넛에서 더 이상 안 봄)
- 백로그: `수주.달성율` 박제 컬럼(아무 코드도 갱신 안 함, 수주현황이 표시 중) 파생 교체 또는 정리
- 자재관리 백로그(#75 후속, 전부 비차단): 칩 키 잔재 플래그 누락(같은 잔량의 새 드럼·잔재 드럼이 한 칩으로 합쳐짐), 동시 출고 초과 사용 DB 가드(Σ사용량≤초기길이 트리거), OutSheet 행 생성 로직 순수함수 추출(유일한 미테스트 비즈니스 로직)

---

## 주요 엔지니어링 결정 (포트폴리오·심층)

> 면접에서 "이거 왜 이렇게 했어요?"라고 물어볼 만한 결정들. 트레이드오프와 데인 지점(함정)을 남긴다.

### 카카오 OAuth 단일 로그인 + 화이트리스트

이메일/비번 로그인을 제거하고 `signInWithOAuth({provider:'kakao'})` 단일 방식으로 전환. 인가는 별도 `whitelist` 테이블(`kakao_id`)로 처리 — `/auth/callback`에서 명단 대조 후 미등록자 거부, 대시보드 레이아웃이 **매 요청 재확인**(퇴사자 즉시 차단). 강제 로그아웃은 서버 컴포넌트가 쿠키를 못 지워 무한루프가 나므로 `/auth/signout` 라우트 핸들러 경유. 명단은 `kakao_whitelist.json` → DB reconcile 스크립트(`npm run sync:whitelist`)로 동기화.

**함정 3종:**
1. **`kakao_id`는 카카오 앱마다 다르다** → 명단을 공유하는 다른 사내 프로그램과 **동일 카카오 앱**을 Supabase 공급자에 연결해야 매칭된다.
2. **GoTrue가 `account_email`+`profile_image`+`profile_nickname` 스코프를 서버에서 강제** 요청(클라 `scopes`로 제거 불가) → 카카오 앱 동의항목 3개 모두 "사용"이어야 한다(이메일은 비즈앱 전환 필요).
3. **`handle_new_user` 트리거가 카카오 NULL 이메일에서 실패** → 합성 이메일 폴백(`<uid>@kakao.local`, `supabase/handle-new-user.sql`).

### RLS 방향 A — 앱 우회 직접 호출 차단

모든 업무 테이블 정책이 `using(true)`라, anon key + 세션만 있으면 앱을 우회해 PostgREST를 직접 호출하면 전 테이블 읽기/변조가 가능한 구멍이 있었다. `is_whitelisted()` SECURITY DEFINER 함수가 **`auth.identities`**(GoTrue 관리·위조 불가)에서 `kakao_id`를 읽어 whitelist와 대조 → 14개 업무 테이블 정책을 `(select is_whitelisted())`로 교체. 쓰기 한 겹 더: `is_admin()`(role='admin')으로 `거래처`·`공사단가`의 insert/update/delete를 admin 전용으로 제한(#60).

**핵심 판단:** `user_metadata`는 사용자가 위조할 수 있으므로 RLS에서 신뢰 금지 — 반드시 `auth.identities` 기준. `whitelist` select 정책은 유지(콜백/레이아웃 본인 조회), service role 경로는 RLS 우회라 무영향. 비상 롤백 스크립트 동봉.

### 데이터 정합성 — 성과금액 일별 증분 정본화

**단독 소스 원칙:** 성과금액은 `공사이력`에만 일별 증분으로 적재한다(매출손익.xlsx는 적재 금지 — 이중적재 사고 방지). 초기엔 읽기 측이 `isMonthEndDate()` 월말 행만 합산해 **성과 82%가 누락**되던 버그가 있었고, 월말 필터를 제거하고 전체 행 합산으로 통일(sales·ProfitChart·revenue·monthly-kpi)해 신ERP `1,952,265,227원`을 복구했다. 구ERP와의 잔여 차이는 수익인식 방식 차이로 확인(신ERP가 진행기준으로 더 정확). 구ERP(매출손익.xlsx) vs 웹ERP 성과·투입금액 **0원 차이** 달성(#41), 오기입 투입실적 정리 포함.

### 변경내역 추적 — DB 트리거 vs 앱레벨 로깅

신규 ERP가 안정화될 때까지 레거시 ERP에 변경을 이중입력해야 해서 "최근 무엇이 들어왔는지"를 빠짐없이 조회할 필요가 있었다.

**앱레벨 로깅 기각 → DB 트리거 채택:** 앱레벨은 10+ mutation 자리를 다 손봐야 하고 하나라도 빠지면 누락된다(특히 행이 사라지는 삭제). 9개 테이블에 `AFTER INSERT/UPDATE/DELETE` 범용 트리거를 걸어 `audit_log`(table_name·operation·row_id·old_data/new_data jsonb·changed_at)에 적재.

**함정 3종:**
1. `audit_log`에 RLS를 켜면 일반 mutation의 트리거 INSERT가 막혀 **원래 작업까지 실패** → 트리거 함수를 `security definer`로 소유자 권한 우회.
2. SELECT(admin) 정책만 두고 INSERT 정책은 두지 않음 → 클라 직접 조작 불가, 기록은 트리거만.
3. `공사이력`엔 생성일 컬럼이 없어 소급입력(작업일자=지난주, 입력=오늘)이 날짜 조회로 안 잡힘 → `changed_at`(실제 insert 시각)이 메운다.

한시적 도구(신규 ERP 단독운영 정착 시 트리거·테이블 드롭). 마이그=`supabase/migrations/20260624120000_audit_log.sql`.

### 날짜 KST 고정

서버가 UTC 런타임이라 `today()`·`formatDate`가 하루 밀리는 버그가 있었다. `src/lib/kst.ts`(`todayKST`·`formatKST`·`partsKST`, Asia/Seoul 고정)를 추가하고 입력/공무/단가/공정 폼의 기본값·기간 계산을 KST로 통일. **함정:** `week.ts`(UTC Date 전용)와 수정일/updatedAt 타임스탬프는 의도적으로 ISO 유지.

### 준공 자동화 + 두 달성률 구분

준공을 찍어도 저장 컬럼 `수주.달성율`을 갱신하는 코드가 없어 달성률이 안 바뀌었고, 준공액≠수주액인데 매출손익은 공사이력 기준이라 정확도가 떨어졌다. 준공 저장 시 `수주.달성율=100` + `공사이력`에 `준공정산=true` 행 한 줄 upsert(성과=준공액공급가−기존누계)로, 매출손익 정본(공사이력 합산)을 안 깨면서 총성과=준공액이 되게 했다. **무손실 해제:** 실수 대비, 해제는 `eq('준공정산',true)`인 자동 행만 삭제(직접 입력 공사이력 보존)+달성율 재계산.

**두 달성률 분리:** 공사이력 기반 공정 달성률(추정치, 100% 초과 허용·clamp 금지)과 수주대장 준공 플래그(=100)는 별개다. 준공은 수동 전용(공사이력 100%가 자동 준공을 트리거하지 않음). 계산은 순수 함수(`completion.ts`)로 분리+단위테스트.

> ⚠️ **#72에서 폐기됨:** 준공정산 자동 적재와 `달성율=100` 쓰기는 달성률 127.19% 고정 버그의 원인으로 판명 — 준공·공사이력 완전 분리(뱃지 표시)로 대체.

### 공사이력 % 입력 — 누적 달성률로 재해석

운영 피드백: `%` 입력칸이 "이번 증분 %"라 65% 공사를 끝내려면 사용자가 `100−65`를 암산해야 했다. 옛 ERP는 `%`칸에 **누적 목표**(30→60→100)를 직접 쳤다. 그 습관을 재현해, `%` 입력을 "이번 작업 후 누적 달성률"로 재해석하고 증분은 시스템이 역산하게 했다(`원` 모드는 증분 그대로 — 규모 큰 공사는 그때그때 시행액을 치는 게 자연스러워 모드별 의미를 분리).

**핵심 판단 — 저장 정본은 여전히 증분(원):** 화면 입력만 누적%로 바꾸고 DB 적재는 행마다 증분 원을 유지했다. 이유는 (1) 성과금액 단일 정본 원칙(`공사이력`만, 원 단위), (2) **기존 데이터 호환** — 과거 행이 전부 증분으로 쌓여 있어 "행마다 누적달성률" 모델로 바꾸면 옛 행엔 그 값이 없어 매출손익 합산이 깨진다. `누적목표를증분으로(누적목표%, base, 누계)` 순수함수(`percent.ts`)로 분리+단위테스트.

**음수 증분 허용:** 현재 60%에서 50%를 입력하면 증분 = 50%−60% = **−10%**(음수)를 저장 → 최종 누계가 입력값(50%)에 정확히 착지. 매출손익은 증분의 월별 합산이라 정정이 일어난 달의 매출이 그만큼 차감(총 누계는 정확) — 의도된 동작.

**100% 초과는 차단 대신 경고:** 옛 ERP는 `0~100` CHECK 제약으로 초과를 막아, 실무자가 초과분을 **준공금액에만** 적어 공사이력과 데이터가 어긋났다. 우리는 제약을 풀되 저장후 100%↑·0%↓에 **비차단 노란 경고**만 띄워(저장은 허용) 오타는 잡고 현실(초과 달성)은 수용한다.

**남은 함정(범위 밖):** 증분-합산 모델은 과거 행을 수정하면 최신 누계가 흔들린다(옛 ERP의 행별 누계-스냅샷과 대비). 이번 입력폼 변경엔 무영향이라 손대지 않음.

### 선택 공사 이력 목록 + 입력칸 % 기준 정리

입력 화면에서 공사를 선택하면 그 공사의 이력을 폼 아래에 **최신순·10개씩** 보여주고(클라이언트 페이지네이션 — `paginate` 순수함수+단위테스트), 행 클릭으로 인라인 수정/삭제한다. 현황 탭에만 있던 수정 Sheet를 `이력수정Sheet` **공용 컴포넌트로 추출**해 현황 탭과 입력 폼이 공유한다. **records lazy-fetch는 호출부 책임**으로 둔다 — 현황 탭은 Sheet를 열 때 fetch하며 `loading`을 넘기고, 입력 폼은 이미 메모리에 가진 `이력목록`을 그대로 넘긴다(`loading=false`).

**입력칸 % 기준 = 전체 누계(누적 목표)로 롤백:** 한때 `%` 기준점을 "작업일자 직전 누계"로 자동 반영해 백필 정확도를 높였으나, **작업일자만 바꿔도 입력칸 값이 자동으로 채워져** 사용자가 혼란스러웠다. 예측가능성을 우선해 전체 누계 기준으로 되돌렸다(작업일자 변경에 입력칸이 반응하지 않음). 트레이드오프로 **과거 날짜 백필을 % 모드로 하면** 빼지는 기준이 그 날짜까지의 누계가 아니라 전체 누계라 증분이 음수로 어긋난다 — 백필은 원(₩) 모드로 입력한다. 단 **수정 Sheet는 자기 자신을 뺀 직전누계 기준을 유지**한다(`직전누계` 순수함수: strict `<` 날짜 비교 + `id` 필터 이중안전 — 작업일자를 바꿔도 자기 자신 제외).

**함정 2종:**
1. 목록 삭제로 현재 페이지가 비면 빈 화면 대신 마지막 페이지를 보여주도록 `paginate`가 `[1, totalPages]`로 clamp.
2. 공사를 바꾸면 페이지 번호가 이전 공사 값으로 남는다 → `<선택공사이력목록 key={선택수주Id}>`로 remount해 page를 1로 리셋(prop 변경만으론 내부 `page` state가 안 리셋됨).

**곁가지 버그 수정:** Δ공정 달성률이 하향 정정으로 음수일 때 `toFixed`의 `-`에 수동 `+`가 겹쳐 `+-64.09%`로 보이던 표기를 양수일 때만 `+`를 붙이도록 수정.

### DB 백업 운영 적용 — IPv6 pooler Docker 풀백업

백업 **코드**는 PR #7로 머지돼 있었지만 **운영에는 안 켜져 있었다**(버킷 0개·실제 백업 0건 = DB 무방비). 2026-06-30 5단계로 실제 가동: ①비공개 버킷 생성 SQL 실행 → ②③VPS `.env.production`에 접속 문자열 추가 → ④수동 1회 실행 검증(Storage 업로드 확인) → ⑤cron `0 3 * * *`(KST) 등록. 매일 새벽 3시 `supabase db dump → gzip → manifest(sha256) → 비공개 Storage 업로드 → 보관정리(일7+주4)`.

**함정 4종(전부 운영 적용에서 처음 드러남):**
1. **직접연결 호스트가 IPv6 전용** — `db.<ref>.supabase.co`가 AAAA 레코드만 응답. 로컬(Windows)·VPS 둘 다 IPv4-only라 연결 자체가 불가. → **Session pooler**(`...pooler.supabase.com:5432`, user `postgres.<ref>`)로 강제. ⚠️ Transaction pooler(6543) 아님 — `pg_dump`는 prepared statement를 써 세션 연결이 필수.
2. **`supabase db dump`가 Docker 필수** — pg_dump를 컨테이너로 실행. VPS에 `docker.io` 설치 필요. CLI는 `npm i -g`가 막혀 **.deb로 `/usr/bin`에 설치**(cron 기본 PATH라 새벽 실행 시 바이너리를 찾음).
3. **Node 20엔 전역 WebSocket이 없다** — supabase-js `createClient`가 realtime을 안 써도 `WebSocket` 생성자를 요구해 throw. `ws`를 `globalThis.WebSocket`에 주입해 회피(가드로 Node 22+ 내장은 안 덮음). `ws`는 **deps**에 둔다(prod install에서 필요).
4. **백업 범위 착각** — 기존 `backup:data` 류는 DB가 아니라 Excel export라 DR과 무관했다. 즉 **DB 행 데이터 백업이 0이었다**. `backup:db`를 schema-only→풀백업으로 확장(schema→data 순서 단일 .sql)해 처음 메움.

**핵심 보안 판단 — 데이터는 `--schema public` 한정:** 데이터 덤프를 public 스키마로 제한해 (a)복원 정합(스키마덤프가 제외하는 auth/storage 테이블에 COPY 시도 방지) (b)**`auth.users` 등 크리덴셜/PII가 백업에 섞이지 않게** 차단. 비번 노출 방지: dump 실패 시 `err.message`(=비번 포함 명령줄)를 절대 출력 안 함.

**남은 공백 — 복원 훈련:** 덤프 *생성·업로드*는 검증됐으나(80KB 풀백업 Storage 확인), 그 `.sql.gz`가 *실제 복원*되는지는 미실시. "복원 안 해본 백업은 백업이 아니다"(`docs/runbook-db-restore.md` 5번) — staging에 `psql -f` 복원 후 행수 대조(`투입실적`/`수주`/`공사이력`)가 다음 액션.

PR #9(머지): ws 주입 + 풀백업 확장. 코드=`scripts/backup-db.ts`, cron=`deploy/backup-db.cron.example`, 런북=`docs/runbook-db-restore.md`.

### 매출손익 성과에서 준공정산 제외

사장님 방침(2026-07-01): 매출손익 하단 **공사별 성과 피벗의 "성과"는 "공사가 실제로 얼마나 진행됐나"만 보여주는 진행분**이어야 한다. 준공 시점의 정산액(준공액−기존누계)까지 성과에 더하면 오히려 헷갈린다는 판단.

**데이터 구조 확인 후 밝혀진 사실 — 실제로 뺄 건 하나뿐:**
- `공사이력.성과금액`엔 두 종류 행이 섞인다: 진행 성과(`준공정산=false`, 일별 증분)와 준공정산 행(`준공정산=true`, 준공완료 시 `OrderForm`이 자동 적재).
- **기성은 애초에 성과에 없었다** — 기성은 `공사이력`이 아니라 별도 `기성` 테이블(`기성액_공급가`)이고, `sales/page.tsx`는 그 테이블을 조회조차 안 한다. "기성+준공을 빼자"에서 실제 제외 대상은 **준공정산 행 하나뿐.**

**구현:** `sales/page.tsx`의 공사이력 쿼리에 `.eq('준공정산', false)` 한 줄 추가. KPI·차트·피벗 세 집계가 모두 이 `공사이력목록` 단일 소스에서 파생되므로, 쿼리 한 곳만 고쳐 **페이지 전체가 일관 제외**(피벗 합계 = KPI = 차트).

**의도된 분기 주의:** 신ERP 공정 달성률(`Σ성과 ÷ 하도적용금액`)은 준공정산 행을 **포함해야** 100%에 도달한다. 이제 매출손익 성과는 준공정산을 빼므로, **준공된 공사에서 "달성률 기준 성과" ≠ "매출손익 성과"** 가 된다 — 버그 아님, 용도가 다름(달성률=완료도, 매출손익=진행 수익인식).

**검증:** 서비스 롤 키로 실데이터 재현 — JY25-098(초평동20-2)은 공사이력 행이 준공정산 하나뿐(9,467,487원)이라 `구 성과 9,467,487 → 신 성과 0(—)`. 2026년 준공정산 4건 합계 약 20,384,183원이 KPI에서 빠짐. `.eq('준공정산', false)` 필터 유무로 각각 합산해 정확히 일치 확인.

> ⚠️ **#72 정정:** 위 "포함해야 100%에 도달"은 실제로는 성립하지 않았다 — 준공정산의 목표(준공액 공급가)와 달성률 분모(하도적용금액)의 기준이 달라 127.19%에 도달했다. 준공정산 행 자체가 #72에서 폐지·삭제됨(필터는 방어용 유지).

### DB 타입 never-collapse 수리 — as any 44곳의 진짜 원인

레포 전체 평가(2026-07-02)에서 시작된 위생 작업. 부수 정리(`migrate:all`의 삭제된 매출손익 스크립트 참조 제거, 루트 목업 → `docs/mockups/`, 레거시 ERP 캡처는 실데이터 우려로 gitignore)와 함께 lint 72개를 파다가 구조적 문제를 발견.

**증상과 오진:** 코드 44곳이 `(supabase.from('테이블') as any)`로 도배돼 있었고, 주석은 "supabase-js 타입 파서가 한국어 컬럼명을 지원하지 않아"였다. 캐스트 하나를 벗겨보니 `.update()` 인자가 `never`로 붕괴 — 한국어 문제가 아니라 **`types/database.ts` 17개 테이블에 `Relationships: []` 키 누락**이 원인이었다. supabase-js는 스키마 타입이 기대 형태와 안 맞으면 전체를 `never`로 무너뜨리는데, 그 44곳은 전부 이 붕괴를 `as any`로 덮은 것. 프로브 파일(미니 스키마에 `Relationships: []`만 추가)로 격리 실험해 확정.

**수리:** 17개 테이블에 키 추가 → `as any` 전량 제거. **insert/update의 컬럼명 오타·타입 실수가 이제 컴파일 타임(에디터 저장 순간)에 잡힌다.** 단 `.select('지중no, 공사명')`처럼 셀렉트 문자열 속 한국어 식별자는 postgrest-js 파서가 진짜로 못 읽어(ParserError), 읽기 38곳은 `as unknown as X` 명시 캐스트로 유지 — 런타임 무변경, 안전 수준은 종전과 동일. **이 캐스트의 보장 주체는 컴파일러가 아니라 "셀렉트 문자열↔타입을 사람이 맞춰둔 약속"** — 문자열을 고치고 타입을 안 고치면 에러 없이 틀린 숫자가 화면에 흐른다(조용한 실패). 위험 인지 상태로 남김.

**함께 한 것:** 한글 컴포넌트 함수명 5개 영문화(`성과Input→PerformanceInput` 등, 파일명은 한글 유지 + 옛 이름 주석) — 린터는 ASCII 대문자 시작만 컴포넌트로 인식해 훅 검사가 통째로 꺼져 있었고, 켜자마자 숨어 있던 set-state-in-effect 6건이 추가로 드러남(2건→8건). 죽은 코드 5건 제거.

**결과:** lint 72→22(에러 62→8), tsc 0, 테스트 75/75, 프로덕션 빌드 통과. 남은 에러 8건은 전부 effect 내 동기 setState — 동작(타이핑 떨림 방지 등)이 걸린 로직이라 다음 작업 단위로 분리(→ #70에서 해소).

### effect 동기 setState 8건 — 파생 상태 수리

#69에서 훅 검사를 켜자 드러난 `react-hooks/set-state-in-effect` 에러 8건(2026-07-02~06, 커밋 7개). 겉보기엔 제각각이지만 전부 "effect로 state를 동기화"하는 같은 병 — effect 동기화는 ①틀린 값으로 한 번 그려진 뒤 고쳐지는 **틀린 프레임**과 ②낭비 리렌더를 만든다. 4유형 프레임으로 건별 분류·수리:

- **①형 파생(저장 금지):** 렌더 중 계산 가능한 값은 state에 두지 않는다. — 페이지 클램프 `safePage`(eab2e64), MoneyInput display 2곳(1f20f83·성과Input — `value.toLocaleString` 렌더 중 계산, 진실의 원천은 부모 value 하나), 로딩 표시 2곳(e34e2ad `fetchedKey !== key`, 4550f5c `조회된수주Id !== 선택수주Id` — **꼬리표에는 "지금 화면의 데이터가 어느 쿼리 입력의 것인지"를 적는다**, 날짜 범위든 수주 id든 쿼리 파라미터가 곧 키).
- **②형 prev 비교 렌더 중 조정(React 공식 패턴):** state가 원본(사용자 입력 초안)인데 prop 변경 시 리셋이 필요한 경우. — 공사명 검색 query(c005b27), 수정 Sheet 초안(c066914), PercentInput display(성과Input — 여러 %가 같은 증분(원)으로 반올림돼 역산이 유일하지 않고 `"12."` 같은 입력 중 문자열도 지켜야 해서 **파생 불가 = 원본**; 타이핑 떨림 방지 가드는 렌더 중 조정 안으로 이식).
- **제4형 정당한 effect(외부 동기화):** 원본이 컴포넌트 밖(DB)에 살면 effect·비동기 적재가 정당 — 동기 setState만 도려낸다. — 이력 fetch 2곳(e34e2ad, 4550f5c `load수주데이터` 분리: 마운트 effect는 fetch만, 동기 리셋은 이벤트 핸들러 전용으로).

**함정 2종:**
1. **lint 규칙은 await 경계를 못 본다**(런타임이 아니라 생김새 판정) — async effect의 await 뒤 setState도 걸린다 → `.then(콜백)` 형태로 옮기면 통과(e34e2ad에서 발견, 이후 표준 패턴으로 재사용).
2. **useState 초기값 중복** — 마운트 effect가 `set선택수주Id(default수주Id)`처럼 초기값과 같은 값을 다시 set하는 코드는 no-op(삭제 대상). 단 `set이력목록([])`의 `[]`·`{}` 리터럴은 매번 새 참조라 no-op조차 아니고 낭비 리렌더를 유발한다.

**결과:** eslint **에러 0**(경고 14 = exhaustive-deps 2 + 미사용 disable 8 + react-compiler 정보성 4), tsc 통과. 부수 효과로 딥링크 진입·수정 Sheet·% 입력칸의 마운트 틀린 프레임 다수 제거. (→ 경고 14건은 #71에서 마감)

### hooks deps 계약 — exhaustive-deps 2건 수리

#70이 남긴 경고 14건 마감(2026-07-06, f465032). 미사용 `eslint-disable(no-explicit-any)` 8건은 `--fix` 자동 제거(#69에서 `as any`를 없애자 주석만 남은 것). 본론은 exhaustive-deps 2건 — **deps 배열 = "이 계산/effect가 읽는 재료 목록" 계약**이고, React는 렌더마다 각 항목을 `Object.is`로 비교해 바뀐 게 있을 때만 재실행한다.

- **HistoryTable `getRow상세`:** 컴포넌트 안 함수는 렌더마다 **새 참조로 다시 태어나** deps에 넣으면 useMemo가 매번 재계산(memo 무력화), 안 넣으면 lint가 추적을 보증 못 함. 해법 = **컴포넌트 밖 순수 함수로 승격**해 재료(`투입구분목록`)를 인자로 받게 — 모듈 로드 때 1번만 생성돼 참조 불변, deps 대상 자체가 소멸. 244줄 경고와 344줄의 exhaustive-deps disable 주석이 동시에 사라짐. 대가: 호출부 5곳이 재료를 명시적으로 전달.
- **OrderForm 공정누계 effect:** lint는 "`row`를 deps에 넣어라"지만 row는 객체(매 렌더 새 참조 가능) — 넣으면 DB 재조회가 반복될 위험. effect가 진짜 읽는 재료는 id 하나뿐이라 `const rowId = row?.id` **원시값으로 좁혀** deps와 일치시킴(재조회는 id가 바뀔 때만, 종전과 동일).

**결과:** 경고 14→4 — 남은 4건은 전부 react-compiler incompatible-library(RHF `watch`·TanStack table 자체 한계, 정보성)로 **조치 가능한 lint 지적 0 도달.** tsc 통과·테스트 75/75. 인지된 백로그: 공정누계 effect는 응답 취소가 없어 빠른 rowId 변경 시 늦은 응답이 덮어쓸 이론적 가능성(빈도 낮아 보류).

### 준공·공사이력 완전 분리 — 달성률 127.19% 버그의 구조적 해소

(2026-07-08, 커밋 9613316)

**증상(2026-07-08):** 공사이력 입력탭에서 준공 공사 여러 건의 달성률이 정확히 127.19%로 동일. 실DB 대조로 원인 확정 — 준공정산 행이 성과 누계를 준공액(대개 =수주액)까지 채우는데, 화면 달성률의 분모는 하도적용금액(수주액×(1−보험료율 7.5%)×하도전용율 85% = 수주액의 78.625%)이라, 준공액=수주액이면 100÷0.78625 = **127.19%가 요율이 같은 모든 공사에서 상수로** 나온다. 병의 정체 = **분자의 100% 기준(준공액)과 분모의 100% 기준(하도적용금액)의 불일치.** DB `수주.달성율`엔 127.19가 없다 — 저장값이 아니라 렌더 시 파생값이었다.

**도메인 재정의:** 준공 = 회계 축(실제 수령 확정 금액), 공사이력 = 진행 축(공사 진행도). **두 축은 서로의 데이터를 변경하지 않는다.** "준공인데 달성률 100% 미만" 위화감은 데이터 조작(강제 채움)이 아니라 **준공 뱃지**로 해소.

**구현(A안):**
- `OrderForm` 준공 저장·해제에서 공사이력 개입 전면 제거 — 준공정산 행 upsert/삭제, `달성율=100`/재계산 쓰기 모두 삭제. `calc준공정산delta`+테스트 제거. 수주폼 달성률 패널의 "준공=100% 고정 표시"(#62)도 실측으로 환원.
- 진행 화면에 `준공Badge` 노출: 입력탭 공사 드롭다운·선택 정보 패널, 현황탭 공사명 옆.
- **데이터 정리:** 기존 자동 준공정산 행 14건을 JSON 스냅샷 후 삭제. 삭제 전 대조에서 **배치별로 다른 공식의 데이터가 섞여 있었음을 확인** — 6~7월 초 처리분 5건은 하도적용금액 기준(화면 100%), 7/7 처리분 등 9건은 준공액 기준(화면 127.19%). 코드 공식은 불변이었으므로 7/2 당시 준공액 입력값이 하도적용 스케일이었던 것으로 추정 — 어느 쪽도 정본이 아니라 전량 삭제가 맞다.
- 매출손익은 #68부터 이 행을 제외해와서 **삭제 영향 0.** `sales`의 `.eq('준공정산',false)` 필터는 백업 복원 등으로 유령 행이 되살아날 경우의 방어용으로 유지.

**남은 축(다음 세션 예약):** "수주대장에 준공액 노출" — 준공액(실수령)이 현재 어떤 보고서에도 안 잡히는 공백 해소. 추가 백로그: `수주.달성율` 저장 컬럼은 이제 아무 코드도 갱신하지 않는 박제 값(수주현황 테이블이 표시 중) — 파생 계산으로 교체 또는 정리 후보.

### 대시보드 유형별 도넛 — 실데이터 연결

(2026-07-09, PR #12)

대시보드에 "유형별 프로젝트 현황" 이중 도넛(안쪽 링=공사구분, 바깥 링=구분×상태) 실데이터 연결. 사장님 피드백 3건(관급→민수 합산, 우상단 요약란, 안쪽 링 비율%) 반영. 구조는 KpiCards 패턴: `_lib/type-status.ts`(집계 순수 함수+vitest) → `TypeStatusDonutSection`(서버: 조회+집계, 1000행 fail-loud 가드) → `TypeStatusDonut`(클라: recharts 렌더).

- **금액 = `calc하도적용표시금액`(관대) 재사용** — 수주대장과 문자 그대로 같은 함수라 합계 일치가 구조적으로 보장. 서비스 키 독립 계산으로 검증(1,024,821만원·547건 일치).
- **상태 = 준공여부 우선** — 이관 데이터의 `시공상태` NULL이 199건(36%)이었고, 모순 조합(완료인데 준공=false 85건 등)도 존재. 준공여부가 실제 플로우(수주대장 필터·준공검사)가 쓰는 컬럼이라 true→완료로 우선 판정, 나머지는 시공상태 매핑, 둘 다 없으면 **미분류를 명시적 회색으로 노출**(안 보이는 것과 분류 안 된 것은 다른 메시지 — 데이터 정리 유도).
- **함정(재발 주의):** recharts Pie가 화면 밖(스크롤 아래)에서 마운트되면 마운트 애니메이션이 중간 프레임에 얼어붙어 도넛이 실오라기로 남는다 → `isAnimationActive={false}`로 회피. 대시보드처럼 폴드 아래 recharts를 둘 때 공통.
- 집계값은 어디에도 저장하지 않고 요청마다 파생 — 별도 집계 테이블은 "원본은 살지만 불일치가 생기는" 달성율 박제와 같은 병이라 기각.

### 자재관리 페이지 — 기록에서 파생하는 재고

(2026-07-13~14, PR #13)

케이블 드럼·기타 자재 재고 관리(`/materials`, 3탭: 케이블 재고 / 기타 자재 / 입·출고 기록). 케이블은 드럼 단위로 입고 → 출고(균등 배분·드럼별 입력·사용량 미기입=출고중) → 이력 시트에서 잔재 복귀. 기타 자재는 스테퍼·직접입력. 내비 승격(사이드바 그룹+구분선, 모바일 탭바 7칸 슬라이드) 포함.

**핵심 결정 3가지:**
- **잔량·수량 컬럼 없음 — 전부 기록에서 파생.** 저장 컬럼은 원본을 고치는 모든 경로가 함께 갱신해야 하고 하나만 빠지면 재고가 조용히 어긋난다 — `수주.달성율` 박제, 도넛 집계 테이블 기각과 같은 병. 테이블 5종(선종·드럼·드럼기록·품목·품목기록)에서 재고/출고중/잔재/피드를 `derive.ts` 순수 함수가 요청마다 계산(vitest 커버).
- **출고중 = 사용량 NULL 모델.** 출고 시점에 사용량을 모르면 NULL로 두고 복귀 때 기입. DB pair constraint `(사용량 is null) = (복귀일 is null)`로 반쪽 상태를 스키마 수준에서 차단.
- **조회는 fail-loud.** PostgREST 1000행 기본 캡이 전체 기록 조회(파생의 입력)를 조용히 자르면 재고 숫자 자체가 틀어진다 — 기존 백로그였던 리스크를 `fetchAll`(페이징+캡 도달 시 throw)로 선제 처리. 선종·품목 조회 실패도 throw — 에러가 삼켜지면 "재고 0"처럼 보이는 그럴듯한 빈 화면이 렌더된다(최종 리뷰 지적으로 발견).

**프로세스 실험 — 서브에이전트 주도 개발:** 플랜 문서에 태스크별 코드 전문을 수록하고, 태스크당 구현 에이전트 1 + 2단계 리뷰(스펙 리뷰=플랜과 바이트 대조, 품질 리뷰) + 지적은 같은 에이전트가 수정 후 재리뷰. 이 게이트에서 리뷰 수정 6건이 잡혔다(입고 드럼수 소수 절삭·날짜 검증, 복귀 동시 저장 진입 가드, 저장 중 탭 전환 전역 비활성, 필터 0건 vs 빈 피드 문구 구분, 사이드바 간격 회귀, 조회 fail-loud).

**함정 2종:**
1. **Tailwind `space-y-*`는 직계 자식에만 닿는다** — 사이드바 링크들을 그룹 div로 감싸자 부모 `<nav>`의 `space-y-0.5`가 그룹 사이만 벌리고 그룹 안 링크끼리는 붙어버렸다(시각 회귀). 그룹 div에도 `space-y-0.5`를 줘야 한다.
2. **E2E 테스트 데이터는 운영 DB에 실제로 쌓인다** — 검증 후 조회로 전 건이 테스트임을 확인하고 **id를 명시해** 삭제(그 사이 생긴 실데이터를 건드리지 않는 안전벨트). 시드(선종·품목)는 유지.

**의도적 트레이드오프(소규모 사무실 수용):** 동시 출고 초과 사용 DB 가드 없음(필요시 트리거로 저렴하게 보강), derive품목 음수 클램프 비대칭(드럼 잔량만 클램프), MSheet 포커스 트랩 없음, router.refresh() 지연.

### 도넛 연도 필터 + 상태 재정의 + 조각 클릭 팝업

(2026-07-14~15, `feat/donut-year-filter` 브랜치 — c77bbcb·906ae32, main 머지 대기)

**연도 필터(c77bbcb):** 수주 테이블엔 수주일 컬럼이 없고 착공일은 45%가 NULL → 지중no 앞 규칙(영문2+연도2)이 유일한 결측 없는 연도 소스. 연도를 못 읽는 행은 `연도미상` 선택지로 노출(조용히 버리면 "전체 ≠ 연도별 합"). 서버 1회 조회 후 연도 전환은 클라 파생(왕복 없음). 기본 연도는 `partsKST()` — 서버 UTC 시계로 `getFullYear()`를 읽으면 KST 00:00~09:00의 9시간 창 동안 전년이 잡힌다.

**상태 재정의(906ae32):** 기존엔 `시공상태` 컬럼(이관 데이터 NULL 199건)으로 분류해 미분류가 부풀었다. 재정의 = **준공여부 → 완료 / 공사이력 ≥1건 → 진행중 / 0건 → 미진행(디폴트)**. 수주만 하고 공사는 나중이 정상 흐름이라 이력 없음의 디폴트는 미분류가 아니라 미진행(사장 확정). 준공+이력 0건도 완료 — 하루짜리 공사는 이력 없이 바로 준공 치는 관행이 흔함. 상태 축 미분류 소멸(유형 축 미분류=공사구분 NULL은 유지). 분포: 552건 → 완료 245 / 진행중 105 / 미진행 202.

**N+1 회피 — `공사이력!수주_id(count)` 임베드(레포 첫 사용):** "수주마다 이력 건수" 조회를 루프로 짜면 1+552=553쿼리(순차 왕복 50ms 기준 ≈27초). 카운트 임베드는 DB가 조인·집계를 해서 쿼리 1방이고, 이력 행을 직접 받지 않으므로 이력 테이블 행수가 몇천이어도 1000행 캡과 무관(캡 가드는 수주 행에 유지). 서비스 롤 읽기 전용으로 실데이터 검증.

**클릭 팝업 — 원본 행 전송 + 단일 원본 파생:** 조각 클릭 시 우측 Sheet에 해당 유형×상태의 공사 목록(지중no·공사명·금액). 서버가 집계 대신 **공사 1건=1행을 통째로 내려주고** 도넛 집계·팝업 목록을 클라가 렌더 중 파생 — "집계+상세 두 벌 전송"은 같은 사실 두 벌이라 도넛과 목록이 어긋날 수 있어 기각(파생값 저장과 같은 병). "집계를 서버에 두면 전송량이 준다"는 논거는 팝업이 원본 목록 자체를 요구하는 순간 뒤집힌다 — 어차피 원본을 보내야 하면 집계 추가 전송은 순수 중복.

---

### 날짜 입력(6/31) — controlled가 스스로 낫는 이유로 수정 범위 11→2

(2026-07-21, `fix/date-field-badinput` — PR 머지)

**증상·원인:** `<input type="date">`에 키보드로 존재하지 않는 날짜(6/31)를 치면 화면엔 보이는데 저장·조회가 조용히 깨진다. 원인 = **브라우저 값 소독(value sanitization)**: 무효 날짜가 조립되면 브라우저가 `input.value`를 빈 문자열로 소독(코드가 읽는 값)하고 `validity.badInput=true`를 켠다. 화면 표시("2026-06-31")와 `value`("")가 어긋나 빈 값/직전값이 조용히 흘러간다.

**설계:** 공용 `DateField`(`src/components/ui/date-field.tsx`) — blur에서 `badInput`을 감지해 입력을 비우고 "존재하지 않는 날짜입니다" 경고. badInput 상태에선 사용자가 친 원문을 읽을 수 없어 31→30 자동교정은 불가, 감지+차단이 최선. `ref`를 일반 prop으로 받아(React 19) register·controlled 둘 다 지원.

**핵심 발견 — 실패 모드가 갈린다(브라우저 실측):** React 19.2 자립 페이지로 실제 Chrome에서 6/31 재현, 콘솔로 이벤트 시퀀스 확인.
- **register(uncontrolled)**: value prop이 없어 무효 상태(`badInput=true, value=""`)가 blur까지 **생존** → DateField가 실제로 감지·비움·경고. 여기가 진짜 버그처.
- **controlled(`value=state`)**: 무효 입력 시 onChange가 소독된 빈 값을 state에 반영하지 않으면(또는 반영해도), **React 기본 복원 동작**이 리렌더 때 DOM을 state 값(가드 있으면 직전 유효값, 없으면 빈값)으로 되돌려 blur 전에 `badInput`을 지운다 → 잘못된 날짜가 저장되지 않고, DateField 감지는 dormant.

**결과:** 게이트1에선 "11곳 전부 교체"로 승인했으나, 실측이 controlled 9곳은 이미 안전함을 보여 **register 2곳(OrderForm 착공일 · InputForm 투입일)만** 교체. 나머지 controlled 9곳은 손대지 않음 — blur-only 하에선 기능 변화 0이고 인라인 레이아웃 리스크만 남기 때문. 검증은 authed 앱 대신 격리 재현으로 대체.

---

## 핵심 기술 결정사항 (코딩 시 필독)

> 프레임워크·라이브러리 특이점과 재사용 가능한 UI 패턴. 다음 코딩 때 또 밟을 함정들.

### Next.js 16 Breaking Changes
- `middleware.ts` → `proxy.ts`, 함수명 `middleware` → `proxy`
- `cookies()` → `await cookies()` (async)
- `searchParams` → `Promise<{...}>` 타입, `await searchParams`로 접근

### Zod v4 API 변경
- 커스텀 에러: `{ message }` → `{ error }`
- `z.string().email()` → `z.email()`
- `z.coerce.number()` 타입 추론 깨짐 → `z.number()` + `setValueAs` 옵션으로 대체
- `@hookform/resolvers v5` 사용 (Zod v4 지원)

### Supabase 한글 테이블명 TypeScript 이슈
- 한글 테이블/컬럼명 → 반환 타입이 `never`로 추론됨
- 해결: `as any` 또는 명시적 타입 캐스팅

```ts
const { data: raw } = await supabase.from('사용자').select()
const user = raw as Pick<사용자Row, '이름'> | null
```

### 인증 — 운영 레퍼런스
- `auth.getSession()` 대신 `auth.getUser()` (서버 측 토큰 검증)
- 표시 이름은 `사용자` 테이블이 아니라 `whitelist.user_name`에서 읽음
- 미로그인 → `/login` 리다이렉트, 강제 로그아웃은 `/auth/signout` 라우트 경유
- 명단 동기화: `npm run sync:whitelist` (JSON은 gitignore — 개인정보)
- DB: `supabase/whitelist.sql`, `supabase/handle-new-user.sql`. `사용자` 테이블은 생성자/수정자 FK 대상이라 유지
- 설계 배경·함정 3종은 → [카카오 OAuth 인증](#카카오-oauth-단일-로그인--화이트리스트)

### 스크롤 레이아웃
- `html/body`에 `h-full` + `overflow-y-auto` 조합 금지 → `min-h-screen` + 자연 window 스크롤 사용

### Table 내 드롭다운 — overflow 클리핑 해결 패턴

`overflow-x-auto` 컨테이너 내부의 `position: absolute` 드롭다운은 컨테이너에 잘린다.
**해결:** `createPortal(dropdown, document.body)` + `position: fixed` + `getBoundingClientRect()`로 위치 지정.

```tsx
const rect = inputRef.current?.getBoundingClientRect()
createPortal(
  <div style={{ position: 'fixed', top: rect.bottom + 2, left: rect.left, zIndex: 9999 }}>
    {items}
  </div>,
  document.body
)
```

항목 클릭 시 input blur 방지: `onMouseDown={e => e.preventDefault()}`.

### SearchableSelect — Radix Dialog 내 portal 드롭다운 3종 버그

**① 클릭 선택 불가** — Radix modal Dialog가 `body.style.pointerEvents="none"` 설정 → portal 클릭 불가.
해결: `DismissableLayerBranch`로 portal 감싸기 + container `pointerEvents:'auto'`, 아이템은 `onMouseDown` preventDefault + `onClick` 선택.

**② 스크롤 시 닫힘** — `document` scroll 리스너(capture)가 드롭다운 내부 스크롤도 감지.
해결: `dropRef`를 컨테이너에 연결, `e.target`이 내부면 early return.

**③ 마우스 휠 차단** — `react-remove-scroll`(Radix Dialog 내장)이 `document` bubble phase `wheel` 리스너로 portal 휠을 `preventDefault()`.
해결: 컨테이너에 `onWheel={e => e.stopPropagation()}`.

---

## 파일 구조 (핵심)

```
src/
├── proxy.ts                          # Next.js 16 세션 갱신 (middleware 대체)
├── lib/
│   ├── supabase/client.ts            # 브라우저용 클라이언트
│   ├── supabase/server.ts            # 서버 컴포넌트용 클라이언트 (async)
│   ├── supabase/admin.ts             # 서비스 롤 클라이언트 (API 전용)
│   ├── whitelist.ts                  # extractKakaoId + getWhitelistEntry
│   ├── whitelist-sync.ts             # reconcileWhitelist 순수 함수
│   └── format.ts                     # formatKRW, formatEok
├── types/database.ts                 # 11개 테이블 TypeScript 타입
└── app/
    ├── login/                        # 카카오 로그인 페이지
    ├── auth/
    │   ├── callback/route.ts         # 카카오 OAuth 콜백 + 화이트리스트 검증
    │   └── signout/route.ts          # 쿠키 정리 로그아웃
    ├── actions/auth.ts               # 로그아웃 Server Action
    └── (dashboard)/
        ├── layout.tsx                # 인증 보호 레이아웃
        ├── admin/layout.tsx          # whitelist.role=admin 관리자 구역 보호
        ├── _lib/calc.ts              # calc투입금액, calc합계
        ├── _components/              # KpiCards, ProfitChart, UnregisteredProjects
        ├── _actions/dashboard.ts     # deleteUnregisteredProject Server Action
        ├── orders/                   # 수주대장 (기본정보/기성/준공 탭)
        ├── input/                    # 투입실적 (InputForm, HistoryTable)
        ├── progress/                 # 공사이력 (ProgressInputForm, ProgressHistoryTable)
        ├── materials/                # 자재관리 (StockTab·EtcTab·LogTab + 시트 3종, _lib/derive.ts)
        └── sales/                    # 매출손익 현황
```

---

## 배포 / 테스트

```bash
# VPS에서
cd /var/www/yjs_erp && ./deploy.sh    # git pull → npm install → build → pm2 reload

# 로컬
npm run dev        # http://localhost:3000
npx tsc --noEmit   # 타입 검사
```

**환경변수 (VPS `.env.production`):**
```
NEXT_PUBLIC_SUPABASE_URL=https://ljwglblarxvhhcogznmf.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...
DASHBOARD_API_KEY=...   # 대시보드 연동 Bearer 토큰
```

**사용자 등록(카카오 로그인):** `kakao_whitelist.json`에 `{kakao_id, user_name, role}` 추가 → `npm run sync:whitelist`. 신입 `kakao_id`는 명단 공유하는 다른 사내 프로그램(동일 카카오 앱)에서 확보.
