# CLAUDE.md — (이 레포 전용 / 프로젝트 스텁)

> 성장 워크플로우(5게이트·티어·캘리브레이션)와 **보강 로그(성장 상태) 정본은 전역 메모리
> `~/.claude/CLAUDE.md`에 있다.** 그 규칙을 그대로 따른다. 여기엔 성장 상태를 두지 않는다.
> 이 파일은 *이 레포에만 해당하는 정보*만 담는다.

---

## 이 프로젝트 컨텍스트

- **프로젝트명:**
- **한 줄 목적:**
- **스택:** (예: Next.js 16 / Supabase / …)
- **실행/배포:** (로컬 실행법, 배포 경로 등)

## 이 레포의 지뢰 / 주의 (게이트에서 우선 캐물을 것)

- (예: 이 레포는 RLS를 쓴다 → 인가 코드 건드릴 때 풀 게이트)
- (예: 회계 정합성이 핵심 → 금액 계산 로직은 무조건 티어 A)
-

## 이 레포 특유의 규칙 / 관례

- (예: 날짜는 전부 `src/lib/kst.ts` 거쳐서 다룬다)
-

## 아키텍처 지도 (⚠️ 내가 직접 손으로 그린다 — Claude가 채우지 않는다)

> 이건 내 머릿속 멘탈 맵의 종이 버전이다. Claude가 채워주면 머리에 안 남는다.
> 전역 §1.5 "구조 장악"의 이 레포 실물. **주 1회: 안 보고 다시 그려본 뒤 → 실제 코드와 대조(diff)해 틀린 곳을 고친다.**
> (예측 적중률 로그는 여기 말고 전역 `~/.claude/CLAUDE.md` §1.5에 쌓는다.)

- **진입점(들):** (예: `app/page.tsx`, API 라우트 `app/api/*`)
- **핵심 모듈과 책임:**
  - `___` → (무슨 일을 하나)
  - `___` →
- **데이터 흐름 (요청 1개가 지나가는 길):**
  1. 사용자가 ___ 하면 →
  2. `___` 파일의 `___` 함수 →
  3. DB / 외부 API `___` →
  4. 응답 `___`
- **핵심 로직이 사는 곳:** (예: 결제 계산 = `src/lib/billing.ts`)
- **아직 지도에서 깜깜한 구역:** (내가 모르는 폴더/파일 = 다음 탐색 대상)
