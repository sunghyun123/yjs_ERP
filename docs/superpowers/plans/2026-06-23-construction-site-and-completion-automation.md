# 공사현장 드롭다운 + 준공 자동화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 수주 등록/수정 시 공사현장을 관리자가 관리하는 드롭다운으로 선택하게 하고, 준공 완료 시 달성률 100%·매출손익 성과(준공액 공급가 기준)를 자동 반영한다.

**Architecture:** 공사현장은 새 테이블에서 옵션만 읽고 `수주.공사현장`은 문자열로 유지(A-1). 준공은 `공사이력`에 `준공정산=true` 행 한 줄을 upsert해 매출손익 정본(공사이력 합산)을 깨지 않고 정산하며, `수주.달성율=100` 플래그를 세팅한다(B-1). 준공 해제는 그 자동 행 하나만 삭제해 사용자 데이터 손실이 코드상 불가능하게 한다.

**Tech Stack:** Next.js(App Router, RSC), Supabase(Postgres+RLS), react-hook-form+zod, TanStack Table, Tailwind, vitest.

**참조 스펙:** `docs/superpowers/specs/2026-06-23-construction-site-and-completion-automation-design.md`

**티어 표기:** 각 Task에 CLAUDE.md 기준 티어를 표시한다. **티어 A** Task는 커밋 전 게이트 4(이해 질문)·게이트 5(사용자가 커밋 메시지 작성)를 적용한다. **티어 B** Task는 Claude가 요약 1~2줄 남기고 커밋 메시지까지 작성한다.

---

## File Structure

**생성:**
- `supabase/migrations/20260623100000_add_construction_site_and_completion.sql` — 공사현장 테이블+RLS+시드, 공사이력.준공정산 컬럼
- `src/app/(dashboard)/orders/_lib/completion.ts` — 준공정산/달성율 순수 계산 함수
- `src/app/(dashboard)/orders/_lib/completion.test.ts` — 위 함수 단위 테스트
- `src/app/(dashboard)/admin/sites/page.tsx` — 공사현장 관리 서버 페이지
- `src/app/(dashboard)/admin/sites/_components/SitesClient.tsx` — 공사현장 추가/삭제 클라이언트

**수정:**
- `src/types/database.ts` — 공사현장 테이블 타입, 공사이력.준공정산 필드, re-export
- `src/components/sidebar/Sidebar.tsx:34-38` — adminNav에 공사현장 링크 추가
- `src/app/(dashboard)/orders/page.tsx` — 공사현장 목록 조회·주입
- `src/app/(dashboard)/orders/_components/OrdersTable.tsx` — 공사현장목록 prop 전달
- `src/app/(dashboard)/orders/_components/OrderForm.tsx` — 공사현장 드롭다운, 준공 저장/해제 로직
- `src/app/(dashboard)/progress/page.tsx:32-36` — 입력용 수주 목록에서 준공 공사 제외

---

## Task 1: DB 마이그레이션 + 타입 (티어 B)

DB 스키마와 TypeScript 타입을 추가한다. 자동화 테스트 없음 — `npm run build`(타입체크)로 검증한다.

**Files:**
- Create: `supabase/migrations/20260623100000_add_construction_site_and_completion.sql`
- Modify: `src/types/database.ts`

- [ ] **Step 1: 마이그레이션 파일 작성**

`supabase/migrations/20260623100000_add_construction_site_and_completion.sql`:

```sql
-- 공사현장: 수주.공사현장 드롭다운 옵션 소스. 수주.공사현장은 문자열로 유지(비정규화, A-1).
-- 공사이력.준공정산: 준공 시 자동 생성되는 정산 행 식별 플래그(B-1).
-- 선행: 20260622090000_rls_whitelist_helper.sql(is_whitelisted), 20260622090300_rls_admin_writes.sql(is_admin)

create table if not exists public.공사현장 (
  id        bigint generated by default as identity primary key,
  현장명     text not null unique,
  생성일     timestamptz not null default now()
);

alter table public.공사현장 enable row level security;

-- 읽기: 화이트리스트 사용자 전체 / 쓰기: admin 전용 (거래처·공사단가와 동일 정책)
create policy 공사현장_select_whitelisted on public.공사현장
  for select to authenticated using ((select public.is_whitelisted()));
create policy 공사현장_insert_admin on public.공사현장
  for insert to authenticated with check ((select public.is_admin()));
create policy 공사현장_update_admin on public.공사현장
  for update to authenticated using ((select public.is_admin())) with check ((select public.is_admin()));
create policy 공사현장_delete_admin on public.공사현장
  for delete to authenticated using ((select public.is_admin()));

-- 시드: 기존 수주.공사현장 distinct 값 + 광명·안양
insert into public.공사현장 (현장명)
select distinct trim(공사현장)
from public.수주
where 공사현장 is not null and trim(공사현장) <> ''
on conflict (현장명) do nothing;

insert into public.공사현장 (현장명) values ('광명'), ('안양')
on conflict (현장명) do nothing;

-- 공사이력 준공정산 플래그
alter table public.공사이력
  add column if not exists 준공정산 boolean not null default false;
```

- [ ] **Step 2: database.ts에 공사현장 테이블 타입 추가**

`src/types/database.ts`의 `Tables` 객체 안, `시스템설정` 정의 바로 앞(또는 뒤)에 추가:

```ts
      공사현장: {
        Row: { id: number; 현장명: string; 생성일: string }
        Insert: { id?: number; 현장명: string; 생성일?: string }
        Update: { id?: number; 현장명?: string; 생성일?: string }
      }
```

- [ ] **Step 3: 공사이력 타입에 준공정산 필드 추가**

`src/types/database.ts`의 `공사이력` 정의에서 세 곳을 수정한다.

Row(현재 `성과금액: number | null` 다음 줄들 사이)에 추가:
```ts
          준공정산: boolean
```
Insert에 추가:
```ts
          준공정산?: boolean
```
Update에 추가:
```ts
          준공정산?: boolean
```

- [ ] **Step 4: 공사현장 Row 타입 re-export 추가**

`src/types/database.ts` 하단 re-export 블록(`export type 계획금액Row ...` 부근)에 추가:

```ts
export type 공사현장Row = Database['public']['Tables']['공사현장']['Row']
```

- [ ] **Step 5: 타입체크 통과 확인**

Run: `npm run build`
Expected: 빌드 성공(타입 에러 없음). 기존 코드가 새 타입과 충돌하지 않아야 한다.

- [ ] **Step 6: 커밋 (티어 B — Claude가 메시지 작성)**

```bash
git add supabase/migrations/20260623100000_add_construction_site_and_completion.sql src/types/database.ts
git commit -m "feat(db): 공사현장 테이블·RLS·시드 + 공사이력.준공정산 컬럼 추가"
```

> **주의:** 이 마이그레이션은 운영 DB에 별도로 적용해야 한다(배포 절차에 따라 `supabase db push` 또는 수동 실행). 적용 전까지 후속 Task의 런타임 동작은 검증되지 않는다.

---

## Task 2: 준공정산·달성율 순수 계산 함수 (티어 A · TDD)

매출손익 정합성의 핵심 계산이므로 순수 함수로 분리해 먼저 테스트한다.

**Files:**
- Create: `src/app/(dashboard)/orders/_lib/completion.ts`
- Test: `src/app/(dashboard)/orders/_lib/completion.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/app/(dashboard)/orders/_lib/completion.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { calc준공정산delta, calc달성율, calc하도적용금액 } from './completion'

describe('calc준공정산delta', () => {
  it('준공액 공급가에서 기존 성과 누계를 뺀 정산액을 반환한다', () => {
    expect(calc준공정산delta(10_000_000, 7_000_000)).toBe(3_000_000)
  })
  it('기존 누계가 준공액보다 크면 음수(하향 정산)를 반환한다', () => {
    expect(calc준공정산delta(5_000_000, 8_000_000)).toBe(-3_000_000)
  })
  it('소수점 둘째 자리로 반올림한다', () => {
    expect(calc준공정산delta(100.005, 0)).toBe(100.01)
  })
})

describe('calc달성율', () => {
  it('성과누계 ÷ 하도적용금액 × 100을 반환한다', () => {
    expect(calc달성율(5_000_000, 10_000_000)).toBe(50)
  })
  it('하도적용금액이 null이면 null', () => {
    expect(calc달성율(5_000_000, null)).toBeNull()
  })
  it('하도적용금액이 0이면 null (0으로 나누기 방지)', () => {
    expect(calc달성율(5_000_000, 0)).toBeNull()
  })
})

describe('calc하도적용금액', () => {
  it('공급가 × (1 − 보험료율) × 하도전용율', () => {
    // 1,000,000 × (1 − 0.075) × 0.85 = 786,250
    expect(calc하도적용금액(1_000_000, 0.075, 0.85)).toBeCloseTo(786_250, 4)
  })
  it('보험료율이 null이면 null', () => {
    expect(calc하도적용금액(1_000_000, null, 0.85)).toBeNull()
  })
  it('하도전용율이 null이면 null', () => {
    expect(calc하도적용금액(1_000_000, 0.075, null)).toBeNull()
  })
  it('공급가가 null이면 null', () => {
    expect(calc하도적용금액(null, 0.075, 0.85)).toBeNull()
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm run test -- completion`
Expected: FAIL — `completion.ts`가 없어 import 에러.

- [ ] **Step 3: 최소 구현 작성**

`src/app/(dashboard)/orders/_lib/completion.ts`:

```ts
// 준공정산 성과 = 목표(준공액 공급가) − 기존 성과 누계(준공정산 행 제외).
// 음수면 하향 정산(기존 누계가 준공액보다 큼). 소수 둘째 자리 반올림(공사이력 적재 관례와 일치).
export function calc준공정산delta(준공액공급가: number, 기존성과누계: number): number {
  return Math.round((준공액공급가 - 기존성과누계) * 100) / 100
}

// 달성율(%) = 성과누계 ÷ 하도적용금액 × 100. 분모가 없거나 0이면 null(0으로 나누기 방지).
export function calc달성율(성과누계: number, 하도적용금액: number | null): number | null {
  if (하도적용금액 == null || 하도적용금액 <= 0) return null
  return (성과누계 / 하도적용금액) * 100
}

// 하도적용금액 = 공급가 × (1 − 보험료율) × 하도전용율.
// 보험료율·하도전용율 중 하나라도 없으면 달성율 분모로 부적절하므로 null.
export function calc하도적용금액(
  공급가: number | null,
  보험료율: number | null,
  하도전용율: number | null,
): number | null {
  if (공급가 == null || 보험료율 == null || 하도전용율 == null) return null
  return 공급가 * (1 - 보험료율) * 하도전용율
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm run test -- completion`
Expected: PASS (모든 케이스).

- [ ] **Step 5: 커밋 (티어 A — 게이트 4 이해 질문 후 사용자가 메시지 작성)**

게이트 4 예시 질문: "calc달성율에서 하도적용금액 0을 null로 처리한 이유는?", "준공정산 delta가 음수가 될 수 있는 상황은 언제이고, 그게 매출손익에 어떻게 반영되나?"

```bash
git add "src/app/(dashboard)/orders/_lib/completion.ts" "src/app/(dashboard)/orders/_lib/completion.test.ts"
# 커밋 메시지는 사용자가 작성 (왜 이 계산을 순수 함수로 분리했는지 중심)
```

---

## Task 3: 공사현장 관리자 페이지 (티어 B)

`/admin/sites`에서 공사현장을 추가/삭제한다. 기존 `/admin/gongmu` 패턴을 따르되 추가·삭제만 둔다(YAGNI: 이름 수정 불필요).

**Files:**
- Create: `src/app/(dashboard)/admin/sites/page.tsx`
- Create: `src/app/(dashboard)/admin/sites/_components/SitesClient.tsx`
- Modify: `src/components/sidebar/Sidebar.tsx:34-38`

- [ ] **Step 1: 서버 페이지 작성**

`src/app/(dashboard)/admin/sites/page.tsx`:

```tsx
import { createClient } from '@/lib/supabase/server'
import type { 공사현장Row } from '@/types/database'
import { SitesClient } from './_components/SitesClient'

export const metadata = { title: '공사현장 관리 | 영전사 ERP' }

export default async function Page() {
  const supabase = await createClient()
  const { data } = await supabase.from('공사현장').select().order('현장명')
  const rows = (data ?? []) as 공사현장Row[]
  return (
    <div className="p-4 md:p-6">
      <div className="mb-5">
        <h1 className="text-xl font-semibold text-gray-900">공사현장 관리</h1>
        <p className="text-sm text-gray-400 mt-0.5">수주 등록 시 선택할 공사현장 목록 · 추가/삭제</p>
      </div>
      <SitesClient initialRows={rows} />
    </div>
  )
}
```

- [ ] **Step 2: 클라이언트 컴포넌트 작성**

`src/app/(dashboard)/admin/sites/_components/SitesClient.tsx`:

```tsx
'use client'

import { useState, useRef, useMemo } from 'react'
import { createClient } from '@/lib/supabase/client'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { CheckCircle2, AlertCircle, X, Plus } from 'lucide-react'
import { cn } from '@/lib/utils'
import type { 공사현장Row } from '@/types/database'

export function SitesClient({ initialRows }: { initialRows: 공사현장Row[] }) {
  const [rows, setRows] = useState(initialRows)
  const [현장명, set현장명] = useState('')
  const [saving, setSaving] = useState(false)
  const [deletingId, setDeletingId] = useState<number | null>(null)
  const [toast, setToast] = useState<{ ok: boolean; msg: string } | null>(null)
  const toastTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null)
  const supabase = useMemo(() => createClient(), [])

  const showToast = (ok: boolean, msg: string) => {
    if (toastTimerRef.current) clearTimeout(toastTimerRef.current)
    setToast({ ok, msg })
    toastTimerRef.current = setTimeout(() => setToast(null), 3000)
  }

  const sortRows = (list: 공사현장Row[]) =>
    [...list].sort((a, b) => a.현장명.localeCompare(b.현장명, 'ko'))

  const handleAdd = async () => {
    const name = 현장명.trim()
    if (!name) { showToast(false, '현장명을 입력하세요.'); return }
    if (rows.some((r) => r.현장명 === name)) { showToast(false, '이미 등록된 현장입니다.'); return }
    setSaving(true)
    const { data, error } = await (supabase.from('공사현장') as any)
      .insert({ 현장명: name }).select().single()
    setSaving(false)
    if (error) { showToast(false, '추가에 실패했습니다.'); return }
    setRows((prev) => sortRows([...prev, data as 공사현장Row]))
    set현장명('')
    showToast(true, '추가되었습니다.')
  }

  const handleDelete = async (row: 공사현장Row) => {
    if (!window.confirm(`'${row.현장명}' 현장을 삭제하시겠습니까?\n기존 수주 데이터에는 영향이 없습니다.`)) return
    setDeletingId(row.id)
    const { error } = await (supabase.from('공사현장') as any).delete().eq('id', row.id)
    setDeletingId(null)
    if (error) { showToast(false, '삭제에 실패했습니다.'); return }
    setRows((prev) => prev.filter((r) => r.id !== row.id))
    showToast(true, '삭제되었습니다.')
  }

  return (
    <>
      {toast && (
        <div className={cn(
          'fixed top-4 right-4 z-50 flex items-center gap-2.5 rounded-xl px-4 py-3 text-sm font-medium text-white shadow-xl',
          toast.ok ? 'bg-green-500' : 'bg-red-500',
        )}>
          {toast.ok ? <CheckCircle2 className="size-4 shrink-0" /> : <AlertCircle className="size-4 shrink-0" />}
          {toast.msg}
          <button type="button" onClick={() => setToast(null)} className="ml-1 opacity-70 hover:opacity-100">
            <X className="size-3.5" />
          </button>
        </div>
      )}

      <div className="flex gap-2 mb-4 max-w-md">
        <Input
          className="h-9 text-sm"
          placeholder="현장명 입력 (예: 광명)"
          value={현장명}
          onChange={(e) => set현장명(e.target.value)}
          onKeyDown={(e) => { if (e.key === 'Enter') handleAdd() }}
        />
        <Button size="sm" onClick={handleAdd} disabled={saving} className="shrink-0">
          <Plus className="size-3.5 mr-1" />
          {saving ? '추가 중...' : '추가'}
        </Button>
      </div>

      <div className="bg-white rounded-xl shadow-sm overflow-hidden max-w-md">
        <table className="w-full text-sm border-collapse">
          <thead>
            <tr className="border-b border-gray-100 bg-gray-50">
              <th className="py-2.5 px-4 text-left text-[11px] font-semibold text-gray-400 uppercase tracking-wide w-12">No</th>
              <th className="py-2.5 px-4 text-left text-[11px] font-semibold text-gray-400 uppercase tracking-wide">현장명</th>
              <th className="py-2.5 px-4 bg-gray-50" />
            </tr>
          </thead>
          <tbody>
            {rows.map((row, i) => (
              <tr key={row.id} className={cn('border-b border-gray-50 hover:bg-blue-50/40', i % 2 === 1 && 'bg-gray-50/50')}>
                <td className="py-2.5 px-4 text-gray-400 tabular-nums text-xs">{i + 1}</td>
                <td className="py-2.5 px-4 font-medium">{row.현장명}</td>
                <td className="py-2.5 px-4 text-right">
                  <button
                    type="button"
                    onClick={() => handleDelete(row)}
                    disabled={deletingId === row.id}
                    className="text-xs border border-gray-200 rounded-md px-2.5 py-1 text-red-500 hover:border-red-400 hover:bg-red-50 transition-colors disabled:opacity-50"
                  >
                    {deletingId === row.id ? '삭제 중...' : '삭제'}
                  </button>
                </td>
              </tr>
            ))}
            {rows.length === 0 && (
              <tr>
                <td colSpan={3} className="py-10 text-center text-sm text-gray-400">등록된 공사현장이 없습니다.</td>
              </tr>
            )}
          </tbody>
        </table>
      </div>
      <p className="mt-2 text-xs text-gray-400">총 {rows.length}개</p>
    </>
  )
}
```

- [ ] **Step 3: Sidebar adminNav에 링크 추가**

`src/components/sidebar/Sidebar.tsx`의 `adminNav` 배열(34–38행)을 수정:

```tsx
const adminNav = [
  { href: '/admin/clients', label: '거래처 관리' },
  { href: '/admin/rates', label: '공사단가 관리' },
  { href: '/admin/gongmu', label: '공무담당자 관리' },
  { href: '/admin/sites', label: '공사현장 관리' },
] as const
```

- [ ] **Step 4: 빌드·동작 확인**

Run: `npm run build`
Expected: 빌드 성공. (런타임 확인: 마이그레이션 적용 후 admin 계정으로 `/admin/sites` 접속 → 추가/삭제 동작.)

- [ ] **Step 5: 커밋 (티어 B — Claude가 메시지 작성)**

```bash
git add "src/app/(dashboard)/admin/sites" src/components/sidebar/Sidebar.tsx
git commit -m "feat(admin): 공사현장 관리 페이지(/admin/sites) 추가·삭제 + 사이드바 링크"
```

---

## Task 4: OrderForm 공사현장 드롭다운 (티어 B)

자유입력 `공사현장`을 드롭다운으로 교체한다. 옵션은 서버에서 주입하고, edit 모드에서 현재 값이 목록에 없으면 그 값도 임시 옵션으로 노출한다.

**Files:**
- Modify: `src/app/(dashboard)/orders/page.tsx`
- Modify: `src/app/(dashboard)/orders/_components/OrdersTable.tsx`
- Modify: `src/app/(dashboard)/orders/_components/OrderForm.tsx`

- [ ] **Step 1: orders/page.tsx에서 공사현장 목록 조회·주입**

`src/app/(dashboard)/orders/page.tsx`의 `Promise.all` 배열에 네 번째 쿼리를 추가하고, 구조분해와 props를 갱신한다.

`Promise.all` 호출을 다음으로 교체:
```tsx
  const [{ data }, { data: 거래처data }, { data: 공무담당자raw }, { data: 공사현장raw }] = await Promise.all([
    supabase
      .from('수주')
      .select(`
        id, 지중no, 공사번호, 공사명, 공사구분, 공사종류, 공사현장,
        작업구분, 시공상태, 준공여부, 착공일, 준공일,
        수주금액_공급가, 준공액_공급가, 달성율, 참고사항,
        보험료율, 하도전용율,
        발주자_id, 원청사_id,
        공사담당, 감독자, 정산상태, 포장여부, 자재청구여부, 공무담당자_id,
        발주자:거래처!발주자_id(거래처명),
        원청사:거래처!원청사_id(거래처명),
        기성(id, 차수, 기성일, 기성액_공급가, 작업내용, 담당공무_id)
      `)
      .order('지중no', { ascending: true }),
    supabase
      .from('거래처')
      .select('id, 거래처명, 보험료제외율, 하도전용율')
      .order('거래처명'),
    supabase
      .from('공무담당자')
      .select('id, 이름')
      .order('이름'),
    supabase
      .from('공사현장')
      .select('현장명')
      .order('현장명'),
  ])
```

`orders`/`거래처목록`/`공무담당자목록` 선언 아래에 추가:
```tsx
  const 공사현장목록 = ((공사현장raw ?? []) as { 현장명: string }[]).map((r) => r.현장명)
```

`<OrdersTable .../>` 호출에 prop 추가:
```tsx
      <OrdersTable data={orders} 거래처목록={거래처목록} 공무담당자목록={공무담당자목록} 공사현장목록={공사현장목록} />
```

- [ ] **Step 2: OrdersTable에서 prop 전달**

`src/app/(dashboard)/orders/_components/OrdersTable.tsx`의 `OrdersTable` props 타입과 시그니처에 `공사현장목록: string[]`을 추가한다.

`export function OrdersTable({ ... })` 시그니처를 교체:
```tsx
export function OrdersTable({
  data,
  거래처목록,
  공무담당자목록,
  공사현장목록,
}: {
  data: 수주행[]
  거래처목록: 거래처목록항목[]
  공무담당자목록: 공무담당자목록항목[]
  공사현장목록: string[]
}) {
```

파일 하단 `<OrderForm .../>` 호출에 prop 추가:
```tsx
            <OrderForm
              mode={formState.mode}
              row={formState.mode === 'edit' ? formState.row : undefined}
              거래처목록={거래처목록}
              공무담당자목록={공무담당자목록}
              공사현장목록={공사현장목록}
              onSuccess={() => setFormState(null)}
            />
```

- [ ] **Step 3: OrderForm Props에 공사현장목록 추가**

`src/app/(dashboard)/orders/_components/OrderForm.tsx`의 `Props` 타입(62–68행)에 추가:
```tsx
type Props = {
  mode: 'new' | 'edit'
  row?: 수주행
  거래처목록: 거래처목록항목[]
  공무담당자목록: 공무담당자목록항목[]
  공사현장목록: string[]
  onSuccess: () => void
}
```

함수 시그니처(272행)를 교체:
```tsx
export function OrderForm({ mode, row, 거래처목록, 공무담당자목록, 공사현장목록, onSuccess }: Props) {
```

- [ ] **Step 4: 공사현장 Input → Select 교체**

`OrderForm.tsx`의 공사현장 Field(현재 663–665행, `<Input ... placeholder="광명" {...register('공사현장')} />`)를 다음으로 교체:

```tsx
                <Field label="공사현장">
                  <Controller
                    name="공사현장"
                    control={control}
                    render={({ field }) => {
                      // edit 모드에서 과거 자유입력 값이 목록에 없으면 임시 옵션으로 노출
                      const opts = field.value && !공사현장목록.includes(field.value)
                        ? [field.value, ...공사현장목록]
                        : 공사현장목록
                      return (
                        <Select
                          value={field.value || '__none__'}
                          onValueChange={(v) => field.onChange(v === '__none__' ? null : v)}
                        >
                          <SelectTrigger className="h-9 text-sm"><SelectValue placeholder="선택" /></SelectTrigger>
                          <SelectContent>
                            <SelectItem value="__none__">—</SelectItem>
                            {opts.map((v) => <SelectItem key={v} value={v}>{v}</SelectItem>)}
                          </SelectContent>
                        </Select>
                      )
                    }}
                  />
                </Field>
```

- [ ] **Step 5: 빌드 확인**

Run: `npm run build`
Expected: 빌드 성공. (런타임: 새 수주/수정 폼에서 공사현장이 드롭다운으로 표시되고, 광명·안양 선택·저장됨.)

- [ ] **Step 6: 커밋 (티어 B — Claude가 메시지 작성)**

```bash
git add "src/app/(dashboard)/orders/page.tsx" "src/app/(dashboard)/orders/_components/OrdersTable.tsx" "src/app/(dashboard)/orders/_components/OrderForm.tsx"
git commit -m "feat(orders): 공사현장 자유입력을 관리 드롭다운으로 교체"
```

---

## Task 5: 준공 자동화 — 저장/해제 로직 (티어 A · 핵심)

준공 완료 시 `달성율=100` + 준공정산 행 upsert, 해제 시 자동 행만 삭제 + 달성율 재계산. 매출손익 정합성·데이터 무손실의 핵심이므로 게이트 전부 적용.

**Files:**
- Modify: `src/app/(dashboard)/orders/_components/OrderForm.tsx`

- [ ] **Step 1: completion 헬퍼 import 추가**

`OrderForm.tsx` 상단 import 블록(27행 `import type { 수주행 ... } from '../_types'` 다음)에 추가:
```tsx
import { calc준공정산delta, calc달성율, calc하도적용금액 } from '../_lib/completion'
```

- [ ] **Step 2: handleJunGongSave 전체 교체**

현재 `handleJunGongSave`(425–440행)를 다음으로 교체:

```tsx
  // 준공 저장/해제 핸들러
  // - 완료: 수주.달성율=100 세팅 + 준공정산 행(준공정산=true) upsert (성과=준공액공급가−기존누계)
  // - 해제: 자동 준공정산 행만 삭제(사용자 공사이력 보존) + 달성율 재계산
  const handleJunGongSave = async () => {
    if (!row) return
    const supabase = createClient()

    // ── 준공 완료 ──────────────────────────────────────────────
    if (준공여부Local) {
      if (!준공일Local || 준공액Local == null) {
        showToast(false, '준공일과 준공액을 모두 입력하세요.')
        return
      }
      set준공저장중(true)

      // 기존 성과 누계(준공정산 행 제외) + 기존 정산행 식별
      const { data: 이력, error: 조회err } = await (supabase.from('공사이력') as any)
        .select('id, 성과금액, 준공정산')
        .eq('수주_id', row.id)
      if (조회err) { set준공저장중(false); showToast(false, '공사이력 조회에 실패했습니다.'); return }

      const 이력목록 = (이력 ?? []) as { id: number; 성과금액: number | null; 준공정산: boolean }[]
      const 기존누계 = 이력목록
        .filter((r) => !r.준공정산)
        .reduce((s, r) => s + (r.성과금액 ?? 0), 0)
      const 기존정산행 = 이력목록.find((r) => r.준공정산) ?? null

      const delta = calc준공정산delta(준공액Local, 기존누계)

      // 하향 정산(기존 누계 > 준공액) 경고
      if (delta < 0 && !window.confirm(
        `기존 성과 누계(${formatKRW(기존누계)})가 준공액(${formatKRW(준공액Local)})보다 큽니다.\n` +
        `성과가 ${formatKRW(delta)}원 하향 조정됩니다. 계속할까요?`
      )) { set준공저장중(false); return }

      // 1) 수주 업데이트 — 달성율 100 플래그
      const { error: 수주err } = await (supabase.from('수주') as any)
        .update({ 준공여부: true, 준공일: 준공일Local, 준공액_공급가: 준공액Local, 달성율: 100 })
        .eq('id', row.id)
      if (수주err) { set준공저장중(false); showToast(false, '준공 저장에 실패했습니다.'); return }

      // 2) 준공정산 행 upsert (작업일자=준공일)
      let 정산err: { message?: string } | null = null
      if (기존정산행) {
        ;({ error: 정산err } = await (supabase.from('공사이력') as any)
          .update({ 작업일자: 준공일Local, 성과금액: delta })
          .eq('id', 기존정산행.id))
      } else {
        ;({ error: 정산err } = await (supabase.from('공사이력') as any)
          .insert({
            수주_id: row.id,
            작업일자: 준공일Local,
            성과금액: delta,
            작업내용: '준공정산(자동)',
            준공정산: true,
            담당공무_id: row.공무담당자_id ?? null,
          }))
      }
      set준공저장중(false)
      if (정산err) { showToast(false, '준공정산 적재에 실패했습니다.'); return }

      showToast(true, '준공 처리 완료 — 달성률 100%·매출손익 반영됨.')
      router.refresh()
      return
    }

    // ── 준공 해제 ──────────────────────────────────────────────
    if (row.준공여부) {
      if (!window.confirm(
        '준공을 해제하면 자동 생성된 준공정산 성과 1건이 제거되고 달성률이 재계산됩니다.\n' +
        '직접 입력하신 공사이력은 그대로 보존됩니다. 계속할까요?'
      )) return
    }
    set준공저장중(true)

    // 1) 자동 준공정산 행만 삭제 (eq 준공정산=true 보장 → 사용자 데이터 손실 경로 없음)
    const { error: 삭제err } = await (supabase.from('공사이력') as any)
      .delete().eq('수주_id', row.id).eq('준공정산', true)
    if (삭제err) { set준공저장중(false); showToast(false, '준공 해제에 실패했습니다.'); return }

    // 2) 남은 성과 누계로 달성율 재계산
    const { data: 남은이력 } = await (supabase.from('공사이력') as any)
      .select('성과금액').eq('수주_id', row.id)
    const 남은누계 = ((남은이력 ?? []) as { 성과금액: number | null }[])
      .reduce((s, r) => s + (r.성과금액 ?? 0), 0)
    const 하도적용 = calc하도적용금액(row.수주금액_공급가, row.보험료율, row.하도전용율)
    const 재계산달성율 = calc달성율(남은누계, 하도적용)

    const { error: 수주err } = await (supabase.from('수주') as any)
      .update({ 준공여부: false, 준공일: null, 준공액_공급가: null, 달성율: 재계산달성율 })
      .eq('id', row.id)
    set준공저장중(false)
    if (수주err) { showToast(false, '준공 해제에 실패했습니다.'); return }

    showToast(true, '준공이 해제되었습니다.')
    router.refresh()
  }
```

- [ ] **Step 3: 빌드·린트 확인**

Run: `npm run build`
Expected: 빌드 성공(타입 에러 없음). `row.공무담당자_id`, `row.준공여부`, `row.보험료율`, `row.하도전용율`, `row.수주금액_공급가`는 `수주행` 타입에 존재한다.

- [ ] **Step 4: 게이트 3 리뷰 (Claude가 비판적 리뷰어로)**

다음 항목을 점검하고 사용자와 수용/반박을 주고받는다:
- 동시성: 두 단계(수주 update → 공사이력 upsert) 사이 실패 시 부분 적용 가능성. 트랜잭션이 아님 → 재저장으로 복구 가능한지.
- delta=0(준공액=기존누계)일 때 0원 정산행이 남는 것이 매출손익/공사이력 현황에 어떻게 보이는지.
- 준공정산 행이 OrderForm 우측 "공정 달성률"(전체 공사이력 합산)에 포함되어 100% 초과 표시될 수 있음 — 스펙상 허용(추정치).

- [ ] **Step 5: 게이트 4 이해 질문 (커밋 전 필수)**

예시: "준공 해제 시 사용자가 직접 입력한 공사이력이 절대 안 지워진다고 보장하는 코드 한 줄은 무엇인가?", "준공을 두 번 저장(재저장)하면 정산행이 중복되지 않는 이유는?", "매출손익 총성과가 정확히 준공액 공급가가 되는 메커니즘을 설명하라."

- [ ] **Step 6: 커밋 (티어 A — 사용자가 메시지 작성)**

```bash
git add "src/app/(dashboard)/orders/_components/OrderForm.tsx"
# 커밋 메시지는 사용자가 작성 ("왜" 중심: 준공정산 행 방식으로 정본을 깨지 않고 정산)
```

---

## Task 6: 공사이력 입력에서 준공 공사 숨김 (티어 B)

`/progress` 입력 탭의 공사 선택 드롭다운에서 준공 완료 공사를 제외한다. 현황(history) 탭은 그대로 둔다.

**Files:**
- Modify: `src/app/(dashboard)/progress/page.tsx:32-36`

- [ ] **Step 1: 수주 쿼리에 준공 필터 추가**

`src/app/(dashboard)/progress/page.tsx`의 수주 조회(32–36행)에 `.eq('준공여부', false)`를 추가:

```tsx
  const { data: 수주raw } = await supabase
    .from('수주')
    .select('id, 지중no, 공사명, 수주금액_공급가, 보험료율, 하도전용율')
    .eq('준공여부', false)
    .order('지중no', { ascending: true })
  const 수주목록 = (수주raw ?? []) as 수주목록항목[]
```

- [ ] **Step 2: 빌드 확인**

Run: `npm run build`
Expected: 빌드 성공. (런타임: 준공 완료된 공사가 `/progress` 입력 드롭다운에 안 보이고, 현황 탭에는 계속 보임.)

- [ ] **Step 3: 커밋 (티어 B — Claude가 메시지 작성)**

```bash
git add "src/app/(dashboard)/progress/page.tsx"
git commit -m "feat(progress): 준공 완료 공사를 공사이력 입력 드롭다운에서 제외"
```

---

## Task 7: 전체 검증 (티어 B)

- [ ] **Step 1: 테스트 전체 실행**

Run: `npm run test`
Expected: 전체 PASS (completion 테스트 포함).

- [ ] **Step 2: 빌드 + 린트**

Run: `npm run build` 그리고 `npm run lint`
Expected: 둘 다 에러 없음.

- [ ] **Step 3: 수동 통합 검증 (마이그레이션 적용된 환경)**

체크리스트:
1. `/admin/sites`에서 현장 추가 → 새 수주 폼 드롭다운에 즉시 반영(페이지 새로고침 후).
2. 준공 안 된 공사를 준공 완료(준공일+준공액 입력) → 수주대장 달성율 100% + 매출손익 해당 공사 총성과 = 준공액 공급가.
3. 같은 공사 준공 해제 → 준공정산 행 사라지고 달성율이 기존 성과 기준으로 돌아옴. **직접 입력한 공사이력은 그대로 남아있는지 확인.**
4. `/progress` 입력 드롭다운에서 준공 공사가 안 보이는지 확인.

- [ ] **Step 4: PROGRESS.md 갱신 (티어 B)**

`PROGRESS.md`에 이번 작업 항목 2건(공사현장 드롭다운/관리, 준공 자동화)을 표 형식에 맞춰 추가하고 커밋.

```bash
git add PROGRESS.md
git commit -m "docs: PROGRESS 업데이트 — 공사현장 드롭다운·준공 자동화"
```

---

## Self-Review (작성자 점검 결과)

**1. 스펙 커버리지:**
- 공사현장 드롭다운(A) → Task 4 ✓ / 관리자 CRUD(A) → Task 3 ✓ / 테이블·RLS·시드(A-1) → Task 1 ✓
- 준공 시 달성률 100%(B) → Task 5(수주.달성율=100) ✓ / 매출손익 준공액 공급가 기준 성과(B) → Task 5(준공정산 행) ✓ / 준공정산 행 식별 boolean(B-1) → Task 1 ✓
- 준공 공사 progress 숨김 → Task 6 ✓ / 준공 해제 무손실 → Task 5(해제 분기, eq 준공정산=true) ✓ / 두 달성률 구분(clamp 금지) → 변경 없음으로 충족(OrderForm 계산값 유지, OrdersTable은 저장 달성율 표시) ✓
- 준공 수동 전용(자동 준공 금지) → 어떤 Task도 공사이력 달성률 기반 자동 준공을 만들지 않음 ✓

**2. Placeholder 스캔:** 모든 코드 스텝에 실제 코드 포함, TBD/TODO 없음 ✓

**3. 타입 일관성:** `calc준공정산delta`/`calc달성율`/`calc하도적용금액`(Task 2 정의) ↔ Task 5 사용 시그니처 일치 ✓. `공사현장Row`(Task 1) ↔ Task 3 사용 일치 ✓. `공사현장목록: string[]`(page→OrdersTable→OrderForm) 경로 일치 ✓. `준공정산` 필드(Task 1 타입) ↔ Task 5 insert/select 사용 일치 ✓.
